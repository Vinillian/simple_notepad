Простой блокнот 📒
Простое веб-приложение для заметок и ссылок с двумя режимами отображения. Работает полностью в браузере, данные сохраняются локально.

✨ Возможности
Заметки и ссылки — можно сохранять как обычный текст, так и ссылки (автоматически определяются по протоколу http/https).

Группы — организуйте элементы по группам с цветовой маркировкой.

Два режима отображения:

Список — компактный вид, для ссылок показывается favicon и домен.

Превью — карточки с изображением, заголовком и описанием для ссылок (метаданные загружаются автоматически).

Поиск по заголовку, содержимому и метаданным ссылок.

Пагинация — подгрузка по 20 элементов, кнопка «Загрузить ещё».

Экспорт/импорт данных в JSON.

Локальное сохранение — всё хранится в localStorage.

Клавиатурные сокращения:

Ctrl+1 — режим списка

Ctrl+2 — режим превью

Ctrl+F — фокус на поиск

Ctrl+Enter — сохранить заметку (из поля ввода)

🛠️ Технологии
HTML5, CSS3, JavaScript (ES6+)

Font Awesome — иконки

Microlink API — получение метаданных для ссылок (превью)

Intersection Observer — ленивая загрузка изображений

localStorage — хранение данных

🚀 Быстрый старт
Склонируйте репозиторий:

bash
git clone https://github.com/Vinillian/simple_notepad.git
Откройте файл index.html в любом современном браузере.

Начните добавлять группы и заметки!

📖 Как пользоваться
Группы
Введите название новой группы в поле «Новая группа...» и нажмите +.

Кликните по группе в списке, чтобы отфильтровать заметки.

Цвет группы выбирается автоматически из набора.

Добавление заметки/ссылки
Заполните поля:

Название — краткий заголовок

Группа — выберите из выпадающего списка

Текст — если это ссылка, она должна начинаться с http:// или https://

Нажмите «Сохранить заметку» или Ctrl+Enter.

Для ссылок автоматически загружаются метаданные (заголовок, описание, изображение). Это может занять несколько секунд после добавления.

Режимы отображения
Кнопки «Список» / «Превью» в шапке или горячие клавиши Ctrl+1 / Ctrl+2.

В режиме списка ссылки отображаются компактно (только favicon и домен).

В режиме превью показывается карточка с изображением и описанием (если доступны).

Поиск
Начните вводить текст в поле поиска — результаты фильтруются по заголовку, содержимому и метаданным ссылок.

Экспорт/импорт
Экспорт — сохраняет все данные (группы и элементы) в JSON-файл.

Импорт — загружает данные из JSON-файла. Можно заменить текущие данные или добавить к существующим.

Управление заметками
✏️ — редактировать (через диалог prompt)

🗑️ — удалить (с подтверждением)

📁 Структура проекта
text
simple_notepad/
├── index.html          # Главная страница
├── style.css           # Стили
├── app.js              # JavaScript-логика (класс NotebookApp)
├── README.md           # Документация
└── *.json              # Примеры экспортированных данных
🔧 Особенности реализации
Все данные хранятся в localStorage под ключом notebookData (настройки вида — notebookSettings).

Для получения превью ссылок используется публичное API microlink.io. Запросы выполняются с очередью (не более одного одновременно) и кешируются.

Изображения в превью загружаются лениво с помощью Intersection Observer.

При импорте данных автоматически обновляются идентификаторы, чтобы избежать конфликтов.

📝 Примечания
Приложение не отправляет данные на сервер (кроме запросов к microlink.io для превью ссылок).

Для работы превью необходимо подключение к интернету.

Если метаданные для ссылки не загрузились, отображается базовая информация (домен, favicon).

📄 Лицензия
Проект распространяется под лицензией MIT. Вы можете свободно использовать и модифицировать код.

Если у вас есть вопросы или предложения, создавайте Issue в репозитории.